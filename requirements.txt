# Core dependencies: harus dapat dipasang di desktop maupun Android/Termux.
discord.py>=2.6,<3
aiohttp>=3.13,<4
gTTS>=2.5,<3
python-dotenv>=1.1,<2
aiosqlite>=0.21,<1
yt-dlp>=2025.1,<2027
tzdata>=2025.2,<2027

# tzdata menyediakan database IANA untuk zoneinfo pada environment yang tidak
# mempunyai system time-zone database lengkap (umum pada Android/Termux).

# Music playback memakai executable FFmpeg dari OS, bukan package Python.
# Termux: pkg install ffmpeg
# Desktop: pastikan ffmpeg tersedia di PATH atau ubah config/music.json.

# Pin Flet server + compiled web client ke versi yang sama untuk menjaga dashboard
# Termux stabil. Browser emoji upload memakai aiohttp + native HTML drag/drop, jadi
# tidak bergantung pada FilePicker Service Flet Web yang bermasalah.
flet==0.86.5
flet-web==0.86.5

# Native Discord voice dependencies. Android/Termux menjalankan degraded mode
# dan melewati dependency ini supaya kegagalan voice tidak memblokir bot utama.
PyNaCl>=1.5,<2 ; sys_platform != "android"
davey>=0.1,<1 ; sys_platform != "android"
discord-ext-voice-recv @ git+https://github.com/imayhaveborkedit/discord-ext-voice-recv.git@ac04ea7b0941112e83767cf1c1469b408fa06748 ; sys_platform != "android"

# Faster Whisper membutuhkan CTranslate2 yang tidak menyediakan wheel Android
# yang kompatibel dengan Termux. Desktop tetap memasangnya secara normal.
faster-whisper==1.2.1 ; sys_platform != "android"
